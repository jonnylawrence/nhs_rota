<p id="notice"><%= notice %></p>

<div class="col-md-8">
  <% if @rotum.Microposts.any? %>
    <h3>Responses (<%= @rotum.Microposts.count %>)</h3>
    <ol class="microposts">
      <%= render @microposts %>
    </ol>
    <%= will_paginate @microposts %>
  <% end %>
</div>

<%= link_to '[Edit]', edit_rotum_path(@rotum) %> |
<%= link_to '[Back]', rota_path %>
<%= link_to '[New Response]', {:controller=>:microposts, :action=>'new', :id => @rotum.id}  %>
