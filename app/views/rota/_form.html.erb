<%= form_for(rotum) do |f| %>
  <% if rotum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rotum.errors.count, "error") %> prohibited this rotum from being saved:</h2>

      <ul>
      <% rotum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Date %>
    <%= f.date_select :Date %>
  </div>

  <div class="field">
    <%= f.label :First_on_call_day %>
    <%= f.text_area :First_on_call_day %>
  </div>

  <div class="field">
    <%= f.label :First_on_call_nights %>
    <%= f.text_area :First_on_call_nights %>
  </div>

  <div class="field">
    <%= f.label :Second_on_call %>
    <%= f.text_area :Second_on_call %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
